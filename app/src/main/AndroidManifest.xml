<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@drawable/ic_pokedox_on"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_pokedox_on"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pokedex"
        tools:targetApi="31">
        <activity
            android:name=".screen.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".feature.login.screen.LoginActivity"/>
        <activity android:name=".feature.detail.screen.DetailActivity"/>
        <activity
            android:name=".screen.MainActivity"
            android:exported="true" />
    </application>

</manifest>